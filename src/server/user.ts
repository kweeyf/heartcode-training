// import {drizzle} from "drizzle-orm/vercel-postgres";
// import {db} from "../../db";

// export default async function inserOneUser(name: string, isDrugDealer:boolean) {
//     await db.insert.(users).values({name: name, isDrugDealer: isDrugDealer});
// }

